const mongoose = require("mongoose");

const userSchema = new mongoose.Schema({
    email: String,
    name: String,
    password: String,
    type: String,
    about_me: String,
    dietary_perferences: Array
});

const userModule = mongoose.model("users", userSchema);

module.exports = userModule