<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
<!-- link navbar.css -->
<link rel="stylesheet" href="navbar.css">

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

<nav class="navbar navbar-dark fixed-top">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar"
            aria-controls="offcanvasNavbar" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="#"><img src="nav_logo.png" class="rounded-circle align-middle"
                style="width:30px; height: 30px;" /></a>
        <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasNavbar"
            aria-labelledby="offcanvasNavbarLabel">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title" id="offcanvasNavbarLabel">Menu</h5>
                <div data-bs-theme="dark">
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
            </div>
            <div class="offcanvas-body">
                <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
                    <% if (!user) { %>
                        <% locals.beforeLoginNav.forEach((link)=> { %>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= link.link %>"><i
                                        class="<%= link.icon %> text-dark p-2"></i>
                                    <%= link.name %>
                                </a>
                            </li>
                            <% }) %>
                                <% } else { %>
                                    <% locals.afterLoginNav.forEach((link)=> { %>
                                        <li class="nav-item">
                                            <a class="nav-link" href="<%= link.link %>"><i
                                                    class="<%= link.icon %> text-dark p-2"></i>
                                                <%= link.name %>
                                            </a>
                                        </li>
                                        <% }) %>
                                            <% } %>

                                                <!-- <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/"><i
                                class='bi bi-house-door-fill text-dark p-2'></i>Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">
                            <i class="bi bi-box-arrow-in-right text-dark p-2"></i>Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup"><i
                                class="bi bi-person-fill-add text-dark p-2"></i>Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact"><i
                                class="bi bi-telephone-outbound-fill text-dark p-2"></i>Contact
                            Us</a>
                    </li> -->
                </ul>
            </div>
        </div>
    </div>
</nav>